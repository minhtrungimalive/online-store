import styled from '@emotion/styled';

const LogoStyled = styled.div`
  width: auto;
  .logo,
  .slogan {
    margin: 0;
    padding: 0;
  }
  .logo {
    font-size: 24px;
    line-height: 1.5;
    /* background-image: linear-gradient(to left, violet, #33ffff, #00ff00, green, yellow, orange, red);
    -webkit-background-clip: text;
    -webkit-background-clip: text;
    -moz-background-clip: text;
    background-clip: text;
    color: transparent; */
  }
  .slogan {
    font-size: 18px;
  }
`;

export default LogoStyled;
